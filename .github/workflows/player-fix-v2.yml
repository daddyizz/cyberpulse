name: Player Fix V2

on:
  push:
    branches:
      - player-fixes-v2

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo player-fixes-v2-ready
